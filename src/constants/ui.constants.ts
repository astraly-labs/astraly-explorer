const UiConstants = {
  SET_PAGE: 'SET_PAGE',
}

export enum PAGES {
  HOME = 'HOME',
  LAUNCHPAD = 'LAUNCHPAD',
}

export const PageTitles: { [key: string]: string } = {
  [PAGES.LAUNCHPAD]: 'Launchpad',
}

export default UiConstants
